<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE properties SYSTEM "http://java.sun.com/dtd/properties.dtd">
<properties>

<!-- 전체 숙소 수 조회 -->
<entry key="getListCount">
SELECT COUNT(*) FROM ROOM
WHERE ROOM_DEL_FL = 'N'
</entry>

<!-- 숙소 목록 조회  -->
<entry key="selectRoomList">
SELECT * FROM
	(SELECT ROWNUM RNUM, R.*
	 FROM (SELECT * FROM ROOM WHERE ROOM_DEL_FL = 'N' AND ROOM_ENROLL_FL = 'Y' ORDER BY ROOM_NO DESC)R)
WHERE RNUM BETWEEN ? AND ?
</entry>

<!-- 숙소 상세 조회 -->
<entry key="selectRoom">
SELECT ROOM_NAME, LOCATION1,LOCATION2,PHONE,ROOM_INFO,CHECKIN,CHECKOUT,FACILITY,DOG,VIEW_COUNT
FROM ROOM
WHERE ROOM_NO=?
AND ROOM_DEL_FL ='N'
AND ROOM_ENROLL_FL ='Y'
</entry>

<!-- 조회 수 증가  -->
<entry key="increaseReadCount">
UPDATE ROOM SET
VIEW_COUNT = VIEW_COUNT +1
WHERE ROOM_NO =?
</entry>

<!-- 다음 번호 얻어오기  -->
<entry key="selectNextNo">
SELECT SEQ_ROOM.NEXTVAL FROM DUAL
</entry>



<!-- 숙소 등록  -->
<entry key="insertRoom">
INSERT INTO ROOM
VALUES(?, ?, ?, ?, ?, ?, ?, ?, ?, ?, DEFAULT, DEFAULT, DEFAULT, ? )
</entry>


<!-- 파일 첨부  -->
<entry key="insertAttachment">
INSERT INTO ROOM_IMG
VALUES(SEQ_ROOMIMG.NEXTVAL,?,?,?,?)

</entry>

</properties>
